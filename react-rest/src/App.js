import './App.css';

function App() {



  return (
    <div className="App">
      <h6> Rest Parameter </h6>
      <p>When the spread operator is used as a parameter, it is known as the rest parameter.
        <br />
        Note: Using the rest parameter will pass the arguments as array elements.
      </p>
    </div>
  );
}

export default App;

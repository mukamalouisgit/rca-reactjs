import React from 'react';

const About = () => {


    return (
        <div>
            <h1>About react wrapper</h1>
            <p>By creating a wrapper component like this, you can encapsulate common styling and apply it consistently around multiple child components, enhancing the visual coherence and reusability of your application.</p>
        </div>
    );
};

export default About;
